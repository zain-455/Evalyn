# Optional: Set this in production builds so the React app knows where the API is.
# In local dev, you can leave it unset to rely on Vite proxy + relative /api URLs.
#
# Examples:
# VITE_API_URL=http://localhost:5230
# VITE_API_URL=https://localhost:7030
VITE_API_URL=
